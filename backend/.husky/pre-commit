#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# ============================================================
# PRE-COMMIT HOOK
# Runs lint-staged to lint and format staged files
# ============================================================

npx lint-staged